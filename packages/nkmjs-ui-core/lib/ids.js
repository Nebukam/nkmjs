'use strict';

const com = require("@nkmjs/common");

/**
 * @description TODO
 * @class
 * @hideconstructor
 * @memberof ui.core
 */
const IDS = {};

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 * @groupdescription These IDs collection are directly referencing the one found in {@link common.IDS}.
 * They are present in IDS for convenience.
 */
IDS.UID = com.IDS.UID;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.STATIC = com.IDS.STATIC;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.NAME = com.IDS.NAME;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.TITLE = com.IDS.TITLE;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.MESSAGE = com.IDS.MESSAGE;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.ICON = com.IDS.ICON;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.IMG = Object.freeze(`img`);

/**
* @description TODO
* @type {string}
* @customtag read-only
* @group Common
*/
IDS.FLAVOR = Object.freeze(`flavor`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.VARIANT = Object.freeze(`variant`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.SIZE = Object.freeze(`size`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.ORDER = Object.freeze(`order`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.PATH = com.IDS.PATH;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.DATA = com.IDS.DATA;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.CMD_PRIMARY = com.IDS.CMD_PRIMARY;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.CMD_SECONDARY = com.IDS.CMD_SECONDARY;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.CMD_LIST = com.IDS.CMD_LIST;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.OWNER = com.IDS.OWNER;

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.ITEM = Object.freeze(`item`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.GROUP = Object.freeze(`group`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.LABEL = Object.freeze(`label`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.SUBTITLE = Object.freeze(`subtitle`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.HEADER = Object.freeze(`header`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.BODY = Object.freeze(`body`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.FOOTER = Object.freeze(`footer`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.VIEW = Object.freeze(`view`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.VIEW_CLASS = Object.freeze(`viewClass`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.CONTROLS = Object.freeze(`controls`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.CSS_CL = Object.freeze(`csscl`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group elements
 */
IDS.TRIGGER = Object.freeze(`trigger`);

/**
 * @description TODO
 * @type {string}
 * @customtag read-only
 * @group Common
 */
IDS.CMD = Object.freeze(`command`);


module.exports = IDS;